import type { NextPage } from "next";
import { Fragment } from "react";
import Hero from "../components/home/Hero";

const Home: NextPage = () => (
  <Fragment>
    <Hero />
  </Fragment>
);

export default Home;
